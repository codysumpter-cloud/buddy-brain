# Memory - 2026-05-07

## Sovereign Cloud / Hostinger repair receipts

### Scope
- Hostinger VPS: `187.77.223.224`
- VPS app root: `/opt/bmo-gateway`
- VPS Hermes home: `/home/hermes/.hermes`
- Telegram cloud buddy bot: `@BeMoreBuddy_bot`
- Cody owner Telegram ID: `7636355145`

### DNS policy correction
- Do **not** change DNS providers or nameservers unless Cody explicitly authorizes it.
- `prismtek.dev` remains on Cloudflare DNS.
- `automindlab.tech` is intended to stay on Hostinger DNS per Cody's direction.
- During this repair session, public authoritative NS still showed Cloudflare nameservers for `automindlab.tech` (`denver`, `elaine`); no effective nameserver change was verified.
- Future agents must treat DNS as a controlled separate task, not as part of app/runtime repair.

### Root cause of Hostinger agent failures
The Hostinger Hermes gateway was alive and replying, but model/provider calls were failing:
- Gemini/Gemma API: HTTP 429 quota exceeded for `gemma-4-31b-it`.
- OpenRouter fallback: HTTP 401 `User not found` for `nvidia/nemotron-3-super-120b-a12b:free`.
- OpenAI key probe: HTTP 401 unauthorized.

### Local model repair
- Added 8 GiB swap on the VPS before local model loading.
- Installed Ollama and enabled `ollama.service`.
- Pulled local small Gemma 4 model: `gemma4:e2b`.
- `ollama list` receipt: `gemma4:e2b`, size about `7.2 GB`.
- Configured Hermes model to local Ollama OpenAI-compatible endpoint:
  - `model.default: gemma4:e2b`
  - `model.provider: custom`
  - `model.base_url: http://127.0.0.1:11434/v1/`
  - `model.max_tokens: 2048`
  - `model.context_length: 64000` to satisfy Hermes minimum context guardrail.
- Configured auxiliary model sections (`vision`, `compression`, `session_search`, `title_generation`) to the same local endpoint to avoid cloud quota/auth failures.

### Verification receipts
- `hermes-gateway.service`: active after restart.
- `ollama.service`: active.
- Direct local Ollama OpenAI-compatible calls returned HTTP 200.
- Direct Hermes CLI smoke test from `/home/hermes` initialized with `gemma4:e2b` and completed a local model response. It replied `LOCAL_OK` to an exact-output prompt, so exact instruction-following is imperfect, but the provider path no longer hits API errors.
- Fresh post-restart gateway log grep showed no new `HTTP 429`, `HTTP 401`, `gemma-4-31b`, `openrouter`, `gemini`, `error`, `failed`, or `traceback` matches.
- Direct bot API send to Cody succeeded: `send_ok True`, `message_id 68`.

### AutoMindLab app receipts
Direct VPS HTTP health checks passed:
- `http://187.77.223.224/healthz` -> `automindlab gateway ok`
- `http://187.77.223.224/api/health` -> `{"status":"ok","service":"automindlab-enterprise-app-factory"}`
- `http://187.77.223.224/diagnostic/api/health` -> `{"status":"ok","service":"automindlab-diagnostic-consultation"}`

Services active on VPS:
- `docker`
- `nginx`
- `automind-diagnostic.service`
- `automind-app-factory.service`
- `ollama.service`
- user service `hermes-gateway.service`

### Known limitations / next checks
- `gemma4:e2b` is CPU-only on a 2-vCPU / 8 GiB VPS and uses swap; expect slow responses.
- The model is a reasoning/thinking model and may spend tokens on reasoning before final content. Keep prompts short and max output high enough.
- Hermes was configured with a 64K advertised context to pass its guardrail, but the local model/runtime may not practically handle a huge real prompt on this VPS. If long Telegram sessions degrade, start a fresh `/new` or reduce tool/context load.
- The remaining true end-to-end test is Cody replying directly to `@BeMoreBuddy_bot` and verifying the gateway-generated response, not just direct bot API send.
